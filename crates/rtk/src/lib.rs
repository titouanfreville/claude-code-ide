//! RTK adapter: wraps session-run commands with RTK for token compression (FR37).
//! Optional integration — its absence degrades features but never breaks the IDE
//! (NFR15). Implemented in build Slice 3.
