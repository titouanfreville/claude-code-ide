{
  "name": "moonlight-control-client",
  "version": "0.1.1",
  "private": true,
  "description": "Typed client for moonlightd's control API. Deliberately free of any `vscode` import so it can be exercised from plain Node.",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "test": "node --test out"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/titouanfreville/claude-code-ide"
  }
}
